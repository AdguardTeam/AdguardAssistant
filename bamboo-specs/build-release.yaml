---
version: 2
plan:
  project-key: ADGEXT
  key: LEGASSRELEASESPECS
  name: legacy assistant - build release

stages:
  - Build:
      manual: false
      final: false
      jobs:
        - Build

Build:
  key: BUILD
  other:
    clean-working-dir: true
  tasks:
    - checkout:
        force-clean-build: true
    - checkout:
        repository: extensions-private
        path: private
        force-clean-build: true
    - script:
        interpreter: SHELL
        scripts:
          - |-
            # 'set' should be added to the beginning of each script to ensure that it runs with the correct options.
            #  -e: Exit immediately if any command exits with a non-zero status (i.e., if a command fails).
            #  -x: Print each command to the terminal as it is executed, which is useful for debugging.
            set -ex

            # Fix mixed logs
            exec 2>&1

            export DOCKER_BUILDKIT=1

            docker build \
              --file Dockerfile \
              --progress plain \
              --build-arg TEST_RUN_ID=${bamboo.buildNumber} \
              --build-context private=private \
              --target build-release-output \
              --output type=local,dest=output \
              .

            # Restore updated dist/ for git commit
            cp -r output/modified/dist/. dist/
    - inject-variables:
        file: output/artifacts/build.txt
        scope: RESULT
        namespace: userscriptMeta
    - any-task:
        plugin-key: com.atlassian.bamboo.plugins.vcs:task.vcs.commit
        configuration:
          # do not change commitMessage as it does not trigger increment after deploy
          commitMessage: 'deploy: update dist v${bamboo.userscriptMeta.version}'
          selectedRepository: defaultRepository
    - any-task:
        plugin-key: com.atlassian.bamboo.plugins.vcs:task.vcs.tagging
        configuration:
          selectedRepository: defaultRepository
          tagName: v${bamboo.userscriptMeta.version}
  final-tasks:
    - script:
        interpreter: SHELL
        scripts:
          - "./bamboo-specs/scripts/cleanup.sh output/artifacts/assistant.meta.js,output/artifacts/assistant.user.js,output/artifacts/assistant.tgz"
  artifacts:
    - name: assistant.meta.js
      location: output/artifacts
      pattern: assistant.meta.js
      shared: true
      required: true
    - name: assistant.user.js
      location: output/artifacts
      pattern: assistant.user.js
      shared: true
      required: true
    - name: assistant.tgz
      location: output/artifacts
      pattern: assistant.tgz
      shared: true
      required: true
  requirements:
    - extension: 'true'

triggers: []

branches:
  create: manually
  delete: never
  link-to-jira: true

labels: []
other:
  concurrent-build-plugin: system-default
